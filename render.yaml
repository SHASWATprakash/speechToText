# Exported from Render on 2026-04-18T15:23:06Z
version: "1"
services:
- type: web
  name: speechToText
  runtime: python
  repo: https://github.com/SHASWATprakash/speechToText
  plan: free
  region: oregon
  buildCommand: pip install -r requirements.txt
  startCommand: uvicorn app:app --host 0.0.0.0 --port 10000
  autoDeployTrigger: commit
